package constant

// 应用配置路径，这里直接取程序所在目录的config.json作为配置
var APP_CONFIG_PATH = "app.json"

var APP_CONFIG_NAME = "app.json"

var PROJECT_CONFIG_NAME = "project.json"

// 自动刷新arg参数名
var APP_AUTO_REFRESH_ARG = "r"

var URI_START_CHAR = "/"